# local-agent-core environment configuration
#
# Copy this file to `.env` and adjust values for your local setup.

# Flask web UI bind address and port
# Host `0.0.0.0` makes the UI reachable from other machines on your LAN.
LAC_WEB_HOST=0.0.0.0
LAC_WEB_PORT=5001

# OpenMemory backend (long-term memory store)
# Base URL for the OpenMemory HTTP API.
OPENMEMORY_URL=http://localhost:8080

# Optional API key if your OpenMemory instance enforces authentication.
# Either of these variable names will be picked up by OpenMemoryStore:
# OPENMEMORY_API_KEY=your_openmemory_api_key_here
# OM_API_KEY=your_openmemory_api_key_here

# Optional legacy base URL variables also supported by OpenMemoryStore:
# OM_BASE_URL=http://localhost:8080

# OBSIDIAN_VAULT_PATH=/path/to/your/vault